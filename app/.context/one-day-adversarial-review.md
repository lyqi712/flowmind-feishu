# 一天优化对抗性审查

**审查时间**: 2026-08-14 12:25 Asia/Shanghai  
**基线**: 389 → **432/432**  
**运行时**: UI `http://127.0.0.1:5179/` + API `http://127.0.0.1:8789`

审查原则：只记可复现证据，不把计划当成已交付。

---

## A. 场景化 Skill

| ID | 场景 | 结果 | 证据 |
|----|------|------|------|
| A1 | 三个决策 Skill 可解析 | 通过 | `q2-planning` / `tech-selection` / `customer-proposal` 在 `SKILLS` 与 `GET /api/skills` |
| A2 | 别名可命中 | 通过 | `Q2规划`、`选型`、`提案` 解析测试绿 |
| A3 | 普通总结未被替换 | 通过 | `summary` 仍在列表；决策 Skill 单独 `category` |
| A4 | 缺材料时仍走现有 Skill 管道 | 通过 | 复用 `executeSkill`，不另开伪造答案通道 |
| A5 | 模型失败不得编造 | 保持 | 沿用既有 502 / 明确错误策略，未加本地假答案 |

残留：三个 Skill 的输入仍是「选中文档 + 一句话」，还不是表单化的 Q1/反馈/竞品槽位。

---

## B. 智能首屏

| ID | 场景 | 结果 | 证据 |
|----|------|------|------|
| B1 | `/api/home` 可返回推荐 | 通过 | 空库返回「同步飞书文档」「创建研究报告」 |
| B2 | 空待办不崩 | 通过 | `todayItems: []` + 默认推荐 |
| B3 | 推荐可一键跑 Skill | 通过 | Chat 空态 `SmartHome` → `run-skill` |
| B4 | 继续会话不会写错 selectedDocs | 通过 | 已改为写入 `documentIds`，不再塞整份文档对象 |
| B5 | 工作台首页 ≠ 聊天空态 | 部分 | 落地页仍是 UnifiedWorkspace 首页；SmartHome 只出现在空聊天 |

残留：启动成本主要降在「新对话」而不是工作台首页。这是有意识的最小切口，不是完整智能首页替换。

---

## C. 飞书输出

| ID | 场景 | 结果 | 证据 |
|----|------|------|------|
| C1 | Markdown → Block | 通过 | 标题/列表/代码/引用单测 |
| C2 | 空内容不炸 | 通过 | 空 markdown 得到 0 block |
| C3 | 对话完成态有导出入口 | 通过 | 助手消息「输出到飞书」 |
| C4 | 未配置/无权限有错误态 | 通过 | 对话框展示 `error.message`，不假装成功 |
| C5 | 不写回飞书原文 | 通过 | 只创建新文档，无 PATCH 原文 |

残留：本环境未做真实飞书创建验收。文件夹接口可能为空，导出仍应落到默认位置并报错可见。

---

## D. 智能搜索

| ID | 场景 | 结果 | 证据 |
|----|------|------|------|
| D1 | 空白页修复 | 通过 | 去掉 Preact；`hotTopics` 改为 `trendingTopics`；ChatWorkspace 补齐 props |
| D2 | 打开搜索层 | 通过 | 对话框 `智能搜索`，热门话题可见，opacity=1 |
| D3 | 历史 API 一致 | 通过 | GET 记录、DELETE 单条、DELETE 清空；不再 POST 不存在的接口 |
| D4 | 热门话题字段 | 通过 | 前端读 `trending` 而非 `topics` |
| D5 | ⌘/Ctrl+K | 代码通过 | 全局快捷键已挂；AX 点击 FAB 偶发不触发 React（与既有工作台按钮同类问题） |

残留：
- 标签名存在编码损坏副本（「标签测试」与乱码各一条）
- 搜索层提交后应关掉并打开结果面板；BrowserPress Enter 未稳定触发 `onKeyDown`

---

## E. 推理链

| ID | 场景 | 结果 | 证据 |
|----|------|------|------|
| E1 | 默认收起 | 通过 | `ReasoningChain` `defaultExpanded=false` |
| E2 | 只在决策 Skill 显示 | 通过 | `shouldShowReasoningChain` 拒绝 `summary` / user |
| E3 | 失败步骤可见 | 通过 | `error`/`stopped` 把进行中步骤标为 failed/cancelled |
| E4 | 普通问答不显示 | 通过 | 非决策 Skill 即使有 steps 也不渲染 |
| E5 | 不伪造思维链 | 通过 | 步骤来自 Skill 工作流事件，不是编造的模型内心独白 |

残留：V1 是工作流步骤可视化，不是模型 token 级 CoT。这是自审时定下的简化版。

---

## F. 回归

| ID | 场景 | 结果 |
|----|------|------|
| F1 | 全量测试 | **432/432** |
| F2 | 阅读器 / 图谱 / 写作既有测试 | 未红 |
| F3 | 模型不可用仍禁止假答案 | 未改这条契约 |
| F4 | 飞书正文只读 | 未增加原文 PATCH |
| F5 | 陆星淇项目隔离 | 未改 `D:\luxiaofei` 其他包 |

---

## 必须诚实的未完成项

1. 批量总结仍受 `selectDocuments(..., limit = 6)` 限制。  
2. 飞书导出未做真实租户创建文档验收。  
3. 390 视口截图本轮 BrowserScreenshot 失败，不能声称移动端视觉验收完成。  
4. 工作台首页还不是 SmartHome。  
5. 未重新打包 1.0.1 安装包。
